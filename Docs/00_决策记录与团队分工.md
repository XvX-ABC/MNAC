# 00 决策记录与团队分工

日期：2026-09-09　分支：cornerstone（隔离基石库，纯 C# + 本地 Unity/MCP 验证环境）

## 背景
基于本分支运动相关代码（`Assets/Scripts/TPhysics/`），做两件事：
1. 分析如何进行**详细测试**。
2. 简化 `LocomotionCore` 中运动**模块的设置**，优先**可视化**。

## 需求确认（AskUserQuestion 四项）
| 问题 | 用户选择 |
|---|---|
| 简化落点形态 | **Editor 面板可视化装配**（SerializeReference + 面板，模块增删/排序/启停可视完成） |
| 落地测试层级 | **PlayMode 物理集成** + **核心类补测试**（GroundDetector/Context/World 等） |
| 是否用 MCP 实跑 | **可以**：PlayMode、tests-run、截图，实跑+截图入档 |
| 公开 API 改动权限 | **允许重构公开 API**（当前唯一消费方是 Tests，风险可控） |

## 交付物（全部中文 markdown，`Docs/`，不主动 git commit）
- `01_需求分析…`（需求-架构 agent）
- `02_装配实现…`（装配实现 agent）
- `03_测试…`（测试实现 agent）
- `04_整合评审…`（组长收尾）

## 团队与调度
- 组长（主线）：调度、MCP 实跑验证（PlayMode / tests-run / 截图）、跨 agent 评审、最终汇总。
- 需求-架构 agent：盘点 → 目标设计 + API 收敛清单 + 里程碑。
- 装配实现 agent：依赖 01，落地面板 + Runner + LocomotionCore 收敛重构。
- 测试实现 agent：依赖 01，落地测试策略 + 核心类测试 + PlayMode 物理集成并跑绿。

调度顺序：`01 需求先行 → 装配实现 ∥ 测试实现 两线并行 → 组长整合评审`。

## 现状快照（开工基线，2026-09-09）
- Environment 重构（GroundDetector 支撑面语义等）已提交落地：`b68eda96` / `1d6f73a7`。
- 结构：`TPhysics/{World,Context}`、`Environment/{EnvironmentSettings,IGroundDetector,Ground,GroundDetector}`、
  `Locomotion/{ILocomotionModule,LocomotionModuleBase,LocomotionCore,Context, 10 行为模块}`、`Tests/LocomotionCore_Test.cs`。
- 装配痛点：`LocomotionCore` 有 `AddModule`/`AddModule_InsertByPriority` 双入口；`Modules` 属性读 `_modules`，
  但 `AddModule`/`RemoveModule`/`EnableModule` 只维护 `_moduleWrappers`，两条数组可能不同步（潜在缺陷，需验证）。
- 既有测试 `LocomotionCore_Test.cs` 有坑：两个 `[SetUp]`、`Random`/`NameToLayer("Terrain")` 误用风险、`[Repeat(5)]` 无种子。
- Unity/MCP：editor 已连接（localhost:22988），PlayMode / tests-run / screenshot 可用；项目可在编辑器里真编译。
- 未提交改动：`MNAC.TPhysics.asmdef`（加回 Utilities/Interaction GUID 引用）等；Interaction/Utilities 程序集已就位。
